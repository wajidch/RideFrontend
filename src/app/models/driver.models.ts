import { RideModel } from './ride.models';

export class DriverModel{
    _id: string;
     firstName: string;
     lastName: string;
     email: string;
     profileName: string;
     city: string;
     address: string;
     dob: string;
     status: string;
     carId: string; 
}